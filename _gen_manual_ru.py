# -*- coding: utf-8 -*-
r"""FILTER - Руководство пользователя (RU). Генератор DOCX на семейственном
шаблоне (_docstyle.py, «вариант C»). Запуск:  python _gen_manual_ru.py
Выход:   docs\FILTER_Manual_RU.docx
"""

import json

import _docstyle as ds

OUT = r'D:\AI\ZCode\Project\FILTER\docs\FILTER_Manual_RU.docx'


def h1(doc, text):
    return ds.h1(doc, text)


def h2(doc, text):
    return ds.h2(doc, text)


def p(doc, text, bullet=False, italic=False, grey=False):
    return ds.p(doc, text, bullet=bullet, italic=italic, grey=grey)


def kv_note(doc, text):
    return ds.kv(doc, text)


def add_table(doc, rows, widths, sev_col=None):
    return ds.add_table(doc, rows, widths, sev_col=sev_col)


def _save(doc, out):
    ds.footer(doc.sections[1], 'FILTER')
    ds.strip_tail(doc)
    doc.save(out)
    h1s = [t for t in ds.H1_REGISTRY if t.lower() not in ('содержание', 'contents')]
    json.dump(h1s, open(out.replace('.docx', '.h1.json'), 'w', encoding='utf-8'),
              ensure_ascii=False)
    print('saved:', out)


doc = ds.new_doc('FILTER', 'Руководство пользователя', 'V1.4.1  -  BLENDER 4.2+')

p(doc, 'FILTER - расширение Blender для быстрого управления видимостью и модификаторами. '
       'Панель показывает объекты сцены, разложенные по типам, коллекциям и именам: любой '
       'тип можно скрыть, закомментировать из рендера или заблокировать для выделения одним '
       'нажатием. Отдельный блок By Modifier управляет модификаторами массово: найти все '
       'Bevel сцены, выделить объекты с ними, применить или удалить - с безопасным порядком '
       'запекания и детектором расхождений.')

kv_note(doc, 'github.com/abyrvalg379/FILTER')

h1(doc, 'Содержание')
ds.toc_field(doc, 'Оглавление: откройте документ в Word/LibreOffice и обновите поле (F9), '
                  'чтобы заполнить номера страниц.')

# ════════════════════════════════════════════════════════════════════════════

h1(doc, '1. О программе')
p(doc, 'Ключевые возможности:', bullet=False)
for b in (
    'видимость, выделение и блокировка по типу объекта: Mesh, Armature, Empty, Light, Camera;',
    'solo-глаз: показать только выбранный тип или коллекцию, спрятать всё остальное;',
    'render-глаз: исключить тип или коллекцию из рендера, не трогая вьюпорт;',
    'фильтр по шаблону имени (без учёта регистра) и по коллекции;',
    'By Modifier: массовое применение, удаление и выделение по группам модификаторов;',
    'детектор расхождения настроек внутри группы модификаторов;',
    'order-safe apply: порядок стаке сохраняется, запекание выполняется только когда нужно;',
    'скип объектов из linked-библиотек и аккуратная работа со связанными дубликатами.',
):
    p(doc, b, bullet=True)

h1(doc, '2. Установка')
for b in (
    'Blender 4.2+: скачайте filter.zip из последнего релиза github.com/abyrvalg379/FILTER → '
    'Preferences → Get Extensions → Install from Disk → выберите zip.',
    'Blender 3.0-4.1: тот же zip, путь Preferences → Add-ons → Install.',
    'Панель появится в N-панели 3D-вьюпорта (клавиша N) → вкладка FILTER.',
):
    p(doc, b, bullet=True)

h1(doc, '3. Быстрый старт')
p(doc, 'Два типовых прохода:')
for b in (
    'Чистый вьюпорт: в блоке By Type нажмите solo-глаз (Shift + клик) на строке Mesh - '
    'останется только геометрия, арматуры, пустышки и свет спрячутся. Повторный Shift + клик '
    'вернёт всё как было.',
    'Массовый Apply: блок By Modifier → выберите группу (например, Bevel) → Apply. Если '
    'модификатор стоит не первым в стаке, FILTER предложит запечь вышележащие - результат '
    'на всех объектах будет ровно таким, как в вьюпорте.',
):
    p(doc, b, bullet=True)

h1(doc, '4. По типу (By Type)')
p(doc, 'Блок раскладывает объекты сцены по пяти типам. У каждого типа - свои кнопки:')
add_table(doc, [
    ('Кнопка', 'Действие'),
    ('Toggle (глаз)', 'скрыть/показать все объекты типа во вьюпорте'),
    ('Select', 'выделить все объекты типа'),
    ('Render (камера)', 'исключить/вернуть тип в рендер, не трогая вьюпорт'),
    ('Lock', 'заблокировать/разблокировать выделение типа'),
    ('Solo (Shift + клик по глазу)', 'показать только этот тип, остальное скрыть; повторно - вернуть'),
], [5.2, 11.8])
p(doc, 'Кнопки состояний друг с другом не конфликтуют: можно, например, держать свет в рендере, '
       'но скрытым во вьюпорте, - рендер-глаз и глаз вьюпорта независимы.')

h1(doc, '5. По имени и по коллекциям')
h2(doc, '5.1 By Name')
p(doc, 'Введите шаблон имени (без учёта регистра) - Toggle скроет или покажет совпавшие '
       'объекты, Select выделит их. Удобно для серийных суффиксов: _geo, _low, LOD*.')
h2(doc, '5.2 Collections')
p(doc, 'Блок показывает коллекции сцены с теми же кнопками видимости, выделения и рендера. '
       'Видимость коллекций ведётся через каналы аутлайнера - то, что выключено в FILTER, '
       'честно выключено и в аутлайнере, и наоборот.')

h1(doc, '6. By Modifier')
p(doc, 'Блок собирает модификаторы всей сцены, сгруппированные по точному имени или по типу '
       '(переключатель группы). Скоуп Scene / Selected ограничивает выбор: вся сцена или '
       'только выделенные объекты.')
h2(doc, '6.1 Операции')
add_table(doc, [
    ('Операция', 'Что делает'),
    ('Select', 'выделить все объекты с модификаторами группы'),
    ('Apply', 'применить модификатор на всех совпавших объектах с order-safe семантикой'),
    ('Remove', 'удалить модификатор группы со всех совпавших объектов'),
    ('Remove disabled', 'одним кликом удалить все выключенные во вьюпорте модификаторы в скоупе'),
    ('Select different', 'найти и выделить объекты, у которых модификатор группы с другими настройками'),
], [4.4, 12.6])
h2(doc, '6.2 Order-safe apply')
p(doc, 'Если применяемый модификатор не первый в стаке, результат зависит от того, что выше '
       'него. FILTER решает это корректно: модификаторы выше целевого сначала запекаются '
       'сверху вниз, и только затем применяется целевой - результат на каждом объекте точно '
       'совпадает с тем, что было во вьюпорте. Диалог подтверждения появляется только когда '
       'запекание действительно требуется.')
h2(doc, '6.3 Особые случаи')
for b in (
    'связанные дубликаты (Alt+D) с общим меш-датаблоком автоматически делаются single-user перед apply;',
    'объекты из linked-библиотек пропускаются - чужие данные не изменяются;',
    'детектор расхождения: строка «3 с другими настройками» означает, что часть модификаторов '
    'группы отличается параметрами - Select different выделит их для разбора.',
):
    p(doc, b, bullet=True)

h1(doc, '7. Как пользоваться')
for t in (
    ('Скриншот чистой модели',
     'Solo-глаз на Mesh - арматура, пустышки и служебные объекты исчезли. Сняли кадр - '
     'Shift + клик вернул сцену.'),
    ('Сдать модель без модификаторов',
     'By Modifier → группа Bevel (или все по типу) → Apply с подтверждением запекания. '
     'Проверьте детектором, что нигде не осталось расхождений, и Remove disabled на всякий случай.'),
    ('Рендер без арматур и пустышек',
     'Render-глаз на Armature и Empty: вьюпорт не меняется, но на кадре их не будет.'),
    ('Разбор чужой сцены',
     'Lock по типам, которые трогать нельзя (Armature), фильтр по имени _geo - и вы работаете '
     'только с тем, что нужно.'),
):
    h2(doc, t[0])
    p(doc, t[1])

h1(doc, '8. Решение проблем')
add_table(doc, [
    ('Симптом', 'Причина', 'Что делать'),
    ('При Apply появляется диалог', 'модификатор не первый в стаке - вышележащие нужно запечь',
     'подтвердите: результат останется ровно как в вьюпорте; без запекания порядок нарушится'),
    ('Часть объектов пропущена при apply', 'объекты из linked-библиотек или multi-user данные',
     'связанные дубликаты делаются single-user автоматически; linked-объекты правятся в своей библиотеке'),
    ('Видимость коллекции «не применяется»', 'канал коллекции выключен в аутлайнере',
     'это единое состояние: проверьте глаз коллекции в аутлайнере - FILTER и аутлайнер синхронны'),
    ('Фильтр имени ничего не находит', 'регистр или лишние символы в шаблоне',
     'фильтр без учёта регистра; проверьте шаблон (поддерживаются подстановки вида *)'),
    ('Объект скрыт, а на рендере виден', 'глаз вьюпорта и render-глаз - независимые состояния',
     'для исключения из рендера используйте render-глаз (иконка камеры)'),
], [4.8, 5.4, 6.8])

_save(doc, OUT)
